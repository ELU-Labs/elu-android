{
  "schemaVersion": 1,
  "baseline": { "platform": "android", "version": "0.1.0", "tag": "0.1.0", "commit": "c5bd7ff7b172748c62d099bdab02b911aee0b0d4", "contractStatus": "provisional-pending-javascript-v1" },
  "domain": "events-lifecycle",
  "normative": false,
  "observations": [
    {
      "id": "pending-buffer-is-bounded-fifo",
      "given": ["the wrapper is pending", "the queue already contains 100 operations"],
      "operations": ["capture one more event"],
      "expected": ["the oldest operation is dropped", "the new operation becomes the FIFO tail", "the queue is memory-only"],
      "evidence": ["source-inspection"]
    },
    {
      "id": "capture-retains-call-time",
      "given": ["capture is called while pending"],
      "operations": ["capture checkout_started"],
      "expected": ["the capture timestamp is created at facade call time", "drain does not replace that timestamp"],
      "evidence": ["source-inspection"]
    },
    {
      "id": "activity-lifecycle-enables-screen-events",
      "given": ["setup receives an Application context", "config enables the runtime"],
      "operations": ["an Activity starts", "the app returns to foreground"],
      "expected": ["activity screen and application lifecycle capture are armed", "stale config is refreshed after 15 minutes"],
      "evidence": ["source-inspection"]
    }
  ],
  "gaps": ["Exact event batches and lifecycle payloads require a captured device/network fixture."]
}
